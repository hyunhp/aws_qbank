@Q001420 cols=3
T Strands Agents runs the reason-act loop: the model reads the tool descriptions and decides at runtime which Python tool to call.
N u users "User" 0 1
N ag bedrock "Strands agent\n(model reasons)" 1 1 ! the agent's model runs on Bedrock
N t1 lambda "Calculator" 2 0 ! Python tools drawn as functions
N t2 lambda "Web search" 2 1 ! Python tools drawn as functions
N t3 lambda "Inventory lookup" 2 2 ! Python tools drawn as functions
E u>ag
E ag>t1 <>
E ag>t2 <> "tool choice"
E ag>t3 <>

@Q001422 cols=2
T A circuit breaker keeps failure counts in DynamoDB and stops calling the throttled endpoint for a cooldown, while bounded Retry and Catch handle the rest.
N sf stepfn "Step Functions\n(bounded Retry/Catch)" 0 0
N cb dynamodb "Circuit state\n(failure counts)" 0 1
N fm sagemaker "FM endpoint\n(throttling)" 1 0 ! the FM endpoint drawn as SageMaker
N fb lambda "Fallback path" 1 1 ! fallback handled by a function
E sf>cb <> "check / update"
E sf>fm "if closed"
E sf>fb -- "if open"

@Q001424 cols=3
T A Lambda step runs the ReAct loop, and Choice states route to whichever tool the model picks until it returns a final answer.
N sf stepfn "Step Functions" 0 1
N re lambda "ReAct step\n(Bedrock call)" 1 1
N s app "Search API" 2 0
N c lambda "Calculator" 2 2
E sf>re
E re>s <> "Choice: search"
E re>c <> "Choice: calc"

@Q001430 cols=3
T A supervisor classifies each request's intent and hands it, with context, to the matching specialized agent.
N u users "Customers" 0 1
N sup bedrock "Supervisor\n(intent classifier)" 1 1 ! agents run on Bedrock models
N a1 bedrock "Billing agent" 2 0 ! agents run on Bedrock models
N a2 bedrock "Tech agent" 2 1 ! agents run on Bedrock models
N a3 bedrock "Orders agent" 2 2 ! agents run on Bedrock models
E u>sup
E sup>a1
E sup>a2 "route"
E sup>a3

@Q001432 cols=3
T A Catch on the failing branch returns a fallback, so the Parallel state completes and aggregation continues with partial results.
N par stepfn "Parallel state" 0 1
N m1 sagemaker "FM A" 1 0 ! FM endpoints drawn as SageMaker
N m2 sagemaker "FM B" 1 1 ! FM endpoints drawn as SageMaker
N m3 sagemaker "FM C (down)\nCatch → fallback" 1 2 ! FM endpoints drawn as SageMaker
N ag lambda "Aggregate" 2 1 ! aggregation drawn as a function
E par>m1
E par>m2
E par>m3
E m1>ag
E m2>ag
E m3>ag -- "fallback"

@Q001436 cols=3
T An inference pipeline chains preprocessing, the large model, and postprocessing containers behind one SageMaker endpoint.
N c app "Client" 0 1
N pre sagemaker "Validate +\nredact PII" 1 0
N mdl sagemaker "Large model" 1 1
N post sagemaker "Format response" 1 2
E c>pre "one endpoint"
E pre>mdl
E mdl>post

@Q001444 cols=2
T The object-created event reaches EventBridge immediately and starts the Step Functions or Lambda pipeline that calls Bedrock, with no polling.
N s3 s3 "Document bucket" 0 0
N eb eventbridge "EventBridge rule" 0 1
N sf stepfn "Pipeline" 1 1
N br bedrock "Bedrock summary" 1 0
E s3>eb "object created"
E eb>sf
E sf>br

@Q001445 cols=2
T Records flow into SQS, and workers pull and call Bedrock at a sustainable rate with retries, which absorbs 100,000 records overnight.
N crm app "CRM export" 0 0
N q sqs "SQS queue" 0 1
N w lambda "Workers" 1 1
N br bedrock "Bedrock" 1 0
E crm>q "100k records"
E q>w
E w>br "summaries"

@Q001447 cols=2
T Okta is federated through IAM Identity Center or Cognito, and each group maps to a role scoped to specific Bedrock models and knowledge bases.
N okta server "Okta IdP" 0 0
N idc idc "Identity Center\nor Cognito" 0 1
N an iam "Analyst role" 1 0
N ad iam "Admin role" 1 1
N br bedrock "Bedrock models,\nknowledge bases" 2 0.5
E okta>idc "SAML"
E idc>an "analyst group"
E idc>ad "admin group"
E an>br "limited"
E ad>br "broader"

@Q001448 cols=2
T Outposts brings AWS infrastructure and APIs into the plant, so inference keeps running locally when the Region link drops.
G plant onprem "Plant floor" 0 0-1
N m server "Machines" 0 0
N op outposts "Outposts\n(local inference)" 0 1
G reg region "AWS Region" 1 0-1
N ctl sagemaker "Management,\nmodel updates" 1 1 ! models are trained and managed in SageMaker
E m>op "low latency"
E ctl>op -- "intermittent link"

@Q001449 cols=2
T Wavelength places compute inside the carrier's 5G network, removing the hops back to a Region for mobile AR users.
N u mobile "AR app (5G)" 0 0
G car generic "Carrier 5G network" 0-1 1
N wz wavelength "Wavelength Zone\ninference" 1 1
G reg region "Parent Region" 2 0-1
N mg app "Rest of the app" 2 1
E u>wz "ultra-low latency"
E wz>mg --

@Q001450 cols=3
T An evaluation stage scores each change against a golden dataset and blocks promotion when quality regresses, with rollback available.
N cb codebuild "Build" 0 0
N ev codebuild "Eval gate\n(golden dataset)" 0 1
N pr lambda "Production" 0 2 ! the agent code runs on Lambda
N cp codepipeline "CodePipeline" 1 1
E cb>ev
E ev>pr "passes"
E cp>ev -- x

@Q001451 cols=2
T The REST API gives each partner an API key and usage plan, validates bodies against a schema, and a WAF web ACL filters abuse before Lambda runs.
N p users "40 partners\n(API keys)" 0 0
N waf waf "WAF web ACL" 0 1
N api apigw "REST API: usage plans,\nrequest validation" 1 0.5
N lam lambda "Summarization\nLambda" 2 0.5
E p>api
E waf>api -- x
E api>lam "valid only"

@Q001453 cols=3
T Each application account assumes a role in the platform account that allows only specific model ARNs and actions.
N d account "Dev account" 0 0
N m account "Marketing account" 0 1
N s account "Support account" 0 2
G plat account "AI platform account" 1-2 0-2
N r iam "Scoped roles\n(model ARNs)" 1 1
N br bedrock "Bedrock" 2 1
E d>r
E m>r "AssumeRole"
E s>r
E r>br

@Q001455 cols=2
T A private hybrid link plus a translation layer turns the agent's REST tool calls into SOAP requests to the on-premises system.
N ag bedrock "Order agent" 0 0
N tr lambda "Lambda: REST → SOAP" 0 1
N dx dx "DX or VPN" 1 1
G dc onprem "On-premises" 2 0-1
N inv server "SOAP inventory" 2 1
E ag>tr "tool call"
E tr>dx
E dx>inv

@Q001457 cols=2
T The user pool federates with the corporate SAML IdP, and the identity pool swaps its tokens for per-user scoped credentials to S3 and the API.
N idp server "Corporate IdP" 0 0
N up cognito "User pool\n(SAML)" 0 1
N ip cognito "Identity pool" 1 1
N br client "Web app" 1 0
N s3 s3 "S3" 2 0
N api apigw "Bedrock-backed API" 2 1 ! the API is fronted by API Gateway
E idp>up "SAML"
E up>ip "tokens"
E ip>br "scoped creds"
E br>s3
E br>api
