@Q001215 cols=2
T Each Region used by a cross-Region action gets its own artifact bucket and KMS key; CodePipeline copies artifacts there, and each key policy lets the reading roles decrypt.
G a region "Pipeline Region" 0-1 0
N cp codepipeline "CodePipeline" 0 0
N b1 s3 "Artifact bucket\n+ KMS key" 1 0
G b region "Deploy Region" 0-1 1
N b2 s3 "Artifact bucket\n+ KMS key" 1 1
N dep cfn "Deploy action" 0 1 ! the deploy stage runs a CloudFormation or similar action
E cp>b1
E b1>b2 "copied"
E b2>dep "decrypt + read"

@Q001216 cols=2
T The buildspec reads the database secret from Secrets Manager at build time, and RDS rotation changes it every 30 days with nothing stored in code.
N cb codebuild "CodeBuild\n(integration tests)" 0 0
N sm secrets "Secrets Manager\n(30-day rotation)" 0 1
N rds rds "RDS MySQL" 1 0.5
E sm>cb "SECRETS_MANAGER\nenv variable"
E cb>rds "connect"
E sm>rds -- "rotate"

@Q001217 cols=2
T A CodeConnections GitHub source action uses the GitHub app and webhooks, so every push to main starts the pipeline with no stored token.
N gh app "GitHub repo\n(main branch)" 0 0
N con app "CodeConnections\n(GitHub app)" 0 1
N cp codepipeline "CodePipeline" 1 1
N cb codebuild "Build stage" 2 1 ! pipelines typically continue to a CodeBuild stage
E gh>con "webhook on push"
E con>cp "source action"
E cp>cb

@Q001219 cols=3
T The manual approval action pauses after staging, notifies approvers through SNS with the staging URL, and only IAM-scoped release managers can approve.
N st cfn "Staging deploy" 0 0 ! stages deploy with an action such as CloudFormation
N ap codepipeline "Manual approval\n(+ staging URL)" 0 1
N pr cfn "Production deploy" 0 2 ! stages deploy with an action such as CloudFormation
N sns sns "SNS notification" 1 1
N rm iam "Release managers\napprove (IAM-scoped)" 2 1
E st>ap
E ap>pr "approved"
E ap>sns
E sns>rm

@Q001228 cols=2
T A CodeBuild webhook runs unit tests on pull request events, and a required status check in GitHub blocks merging when they fail.
N pr app "GitHub pull request" 0 0
N cb codebuild "CodeBuild\n(unit tests)" 0 1
N st app "Required status\ncheck" 1 0
E pr>cb "PR created/updated"
E cb>st "pass / fail"
E st>pr -- "blocks merge"

@Q001232 cols=2
T A Lambda invoke action runs the custom test call and reports the verdict back to CodePipeline with PutJobSuccessResult or PutJobFailureResult.
N cp codepipeline "CodePipeline" 0 0
N lam lambda "Lambda invoke\naction" 0 1
N api app "Test orchestration\nAPI" 1 1
E cp>lam "job"
E lam>api "run tests"
E lam>cp -- via=0.6,1;0.6,0 "PutJobSuccess /\nFailureResult"

@Q001240 cols=3
T Sequential stages promote a build only after it passes each environment's gate: unit tests in dev, then unit, integration, and load tests in staging.
N dev cfn "Dev\n(unit tests)" 0 0 ! each stage deploys with an action such as CloudFormation
N stg cfn "Staging (unit,\nintegration, load)" 0 1 ! each stage deploys with an action such as CloudFormation
N prd cfn "Production" 0 2 ! each stage deploys with an action such as CloudFormation
N cp codepipeline "CodePipeline" 1 1
E dev>stg "passed"
E stg>prd "passed"
E cp>dev -- x

@Q001241 cols=2
T CodeArtifact serves approved internal packages first and, through the external connection, proxies npmjs and Maven Central for everything else.
N cb codebuild "Developers /\nbuilds" 0 0.5 ! package consumers such as CodeBuild
N repo codeartifact "CodeArtifact repo" 1 0.5
N int codeartifact "Internal upstream\n(approved versions)" 2 0
N ext internet "npmjs / Maven\nCentral" 2 1
E cb>repo
E repo>int "1. internal"
E repo>ext "2. external\nconnection"

@Q001245 cols=3
T An Image Builder pipeline hardens and tests the monthly AMI, then distributes it to 3 Regions and 20 accounts and updates their launch templates.
N ib imagebuilder "Image Builder\n(patch, CIS, test)" 0 1
N r1 ec2 "Region 1\nlaunch template" 1 0 ! distributed AMIs are used by EC2 launch templates
N r2 ec2 "Region 2\nlaunch template" 1 1 ! distributed AMIs are used by EC2 launch templates
N r3 ec2 "Region 3\n(20 accounts)" 1 2 ! distributed AMIs are used by EC2 launch templates
E ib>r1
E ib>r2 "distribute"
E ib>r3

@Q001248 cols=2
T ECR private registry replication copies each image pushed in us-east-1 to eu-west-1, where that Region's ECS cluster pulls it.
G e region "us-east-1" 0-1 0
G w region "eu-west-1" 0-1 1
N r1 ecr "ECR registry" 0 0
N r2 ecr "ECR replica" 0 1
N c1 ecs "ECS cluster" 1 0
N c2 ecs "ECS cluster" 1 1
E r1>r2 "replication" @0,0.5
E r1>c1
E r2>c2

@Q001253 cols=2
T Images in the shared repository use Account A's CMK; the key policy lets Account B decrypt and the repository policy lets it pull.
G a account "Account A" 0-2 0
N cb codebuild "Pipeline build" 0 0 ! the pipeline builds images with CodeBuild
N repo ecr "ECR repo\n(repo policy)" 1 0
N key kms "CMK (key policy\nallows B)" 2 0
G b account "Account B" 0-2 1
N ecs ecs "ECS service" 1 1
E cb>repo "push"
E ecs>repo "pull"
E ecs>key -- "decrypt"

@Q001254 cols=2
T CodeDeploy copies the Auto Scaling group, deploys and validates the new fleet, shifts ALB traffic, and keeps the old fleet for instant rollback.
N alb alb "ALB" 0 0.5
N blue asg "Original ASG\n(kept for rollback)" 1 0
N green asg "Replacement ASG\n(validated)" 1 1
N cd codedeploy "CodeDeploy\nblue/green" 2 0.5
E alb>blue -- hv "before"
E alb>green hv "after shift"
E cd>green -- x

@Q001256 cols=2
T The Lambda alias sends 10% to the new version for 10 minutes and shifts the rest only if no CloudWatch alarm fires.
N cd codedeploy "CodeDeploy canary\n10% for 10 min" 0 0.5
N v1 lambda "Current version" 1 0
N v2 lambda "New version" 1 1
N cw cloudwatch "Alarms" 2 1
E cd>v1 hv "90%"
E cd>v2 hv "10% → 100%"
E cw>v2 -- x "watches errors"

@Q001260 cols=2
T The new version goes to a separate bucket and is validated; then the CloudFront origin is switched, keeping the old bucket for instant rollback.
N u users "Users" 0 0.5
N cf cloudfront "CloudFront" 1 0.5
N blue s3 "Current bucket\n(rollback)" 2 0
N green s3 "New version\nbucket (validated)" 2 1
E u>cf
E cf>blue -- hv "old origin"
E cf>green hv "switched origin"

@Q001261 cols=2
T EFS is a shared file system, so the green fleet mounts the same uploads; check mount settings and NFS security group rules for the new instances.
N blue ec2 "Blue fleet" 0 0
N green ec2 "Green fleet" 0 1
N efs efs "Amazon EFS\n(user uploads)" 1 0.5
E blue>efs <>
E green>efs <> "NFS 2049\nallowed"

@Q001271 cols=2
T AppConfig rolls the new pricing setting out gradually, watches CloudWatch alarms, and rolls back automatically if one fires.
N ac appconfig "AppConfig\n(gradual strategy)" 0 0.5
N lam lambda "Lambda API" 1 0
N cw cloudwatch "CloudWatch alarm\nmonitor" 1 1
E ac>lam "config in steps"
E cw>ac -- "roll back" via=0.5,1
