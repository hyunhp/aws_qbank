@Q000588 cols=2
T The API accepts the upload and returns a request ID immediately; workers process the video asynchronously and the client polls or gets notified.
N app mobile "Mobile app" 0 0
N api apigw "Upload API" 0 1 ! the upload endpoint shown as API Gateway
N q sqs "Job queue" 1 1 ! asynchronous work is queued, for example in SQS
N w lambda "Transcode /\nthumbnail workers" 2 1 ! workers drawn as Lambda
N st dynamodb "Job status" 2 0 ! job status kept in a table
E app>api <> "1. upload\n2. request ID"
E api>q
E q>w
E w>st "done"
E app>st -- "3. poll"

@Q000601 cols=2
T The function's private subnets route internet-bound traffic to a NAT gateway in a public subnet, while RDS stays reachable inside the VPC.
N net internet "Stripe API" 0 0.5
G vpc vpc "VPC" 1-2 0-1
G pub public-subnet "Public subnet" 1 0-1
N nat nat "NAT gateway" 1 0.5
G prv private-subnet "Private subnets" 2 0-1
N lam lambda "Lambda (VPC)" 2 0
N rds rds "RDS" 2 1
E lam>nat "0.0.0.0/0"
E nat>net
E lam>rds

@Q000602 cols=2
T A VPC-attached function has only private IPs, so it needs a route to a NAT gateway or a VPC endpoint to reach services such as DynamoDB.
G vpc vpc "VPC" 0-1 0-1
G prv private-subnet "Private subnets" 0 0-1
N lam lambda "Lambda (VPC)" 0 0
N ec elasticache "ElastiCache" 0 1
N gep endpoint "DynamoDB endpoint\n(or NAT route)" 1 0
N ddb dynamodb "DynamoDB" 2 0
E lam>ec
E lam>gep
E gep>ddb

@Q000606 cols=2
T S3 sends object events to EventBridge; a suffix-filtered rule drives the thumbnailer and a second rule feeds the audit pipeline, each retrying independently.
N s3 s3 "Upload bucket" 0 0.5
N eb eventbridge "EventBridge" 1 0.5
N th lambda "Thumbnailer\n(.jpg only)" 2 0
N au app "Audit pipeline\n(all objects)" 2 1
E s3>eb
E eb>th "rule 1"
E eb>au "rule 2"

@Q000629 cols=2
T ElastiCache serves repeated catalog reads from memory (cache-aside), cutting DynamoDB read costs and latency.
N app app "Web application" 0 0.5
N c elasticache "ElastiCache" 1 0
N ddb dynamodb "DynamoDB catalog" 1 1
E app>c hv "1. read"
E app>ddb -- hv "2. on miss"

@Q000630 cols=2
T DynamoDB stays the source of truth, and its changes stream into OpenSearch, which handles fuzzy full-text search, ranking, and facets.
N app app "Catalog writes" 0 0
N ddb dynamodb "DynamoDB" 0 1
N st ddbstream "Streams" 1 1
N lam lambda "Sync function" 2 1 ! a function or zero-ETL keeps the index in sync
N os opensearch "OpenSearch index" 2 0
N u users "Search users" 1 0
E app>ddb
E ddb>st
E st>lam
E lam>os
E u>os "fuzzy search"

@Q000631 cols=2
T Changes stream from DynamoDB into OpenSearch, which provides typo-tolerant, ranked full-text search over the product descriptions.
N ddb dynamodb "DynamoDB" 0 0
N st ddbstream "Streams" 0 1
N lam lambda "Sync function" 1 1 ! a function keeps the index in sync
N os opensearch "OpenSearch" 1 0
E ddb>st
E st>lam
E lam>os

@Q000634 cols=2
T The identity pool trusts the user pool and exchanges the user's token for temporary credentials from a scoped IAM role to upload to S3.
N app mobile "Mobile app" 0 0
N up cognito "User pool" 0 1
N ip cognito "Identity pool" 1 1
N role iam "Scoped IAM role" 2 1
N s3 s3 "S3 bucket" 1 0
E app>up "sign in"
E up>ip "token"
E ip>role
E app>s3 "upload with\ntemp creds"

@Q000644 cols=2
T Service A's task role signs requests with SigV4, and the API method's IAM authorization allows only that role.
N a ecs "Service A\n(task role)" 0 0
N api apigw "API Gateway\n(IAM auth)" 0 1
N b ecs "Service B" 1 1
E a>api "SigV4-signed"
E api>b

@Q000652 cols=3
T AWS RAM shares the central Private CA, so member accounts issue certificates from it without their own CA.
G sh account "Shared services account" 0 0-2
N ca pca "Private CA" 0 1
N ram ram "RAM share" 0 2
N a1 account "Member account" 1 0
N a2 account "Member account" 1 1
N a3 account "Member account" 1 2
E ram>ca -- x
E a1>ca "issue certs"
E a2>ca
E a3>ca

@Q000690 cols=3
T Each stage sets a stage variable that the integration ARN uses, so dev, test, and prod call different aliases of the same function.
N d apigw "dev stage" 0 0
N t apigw "test stage" 0 1
N p apigw "prod stage" 0 2
N a1 lambda "Alias dev" 1 0
N a2 lambda "Alias test" 1 1
N a3 lambda "Alias prod" 1 2
E d>a1 "${stageVariables\n.alias}"
E t>a2
E p>a3

@Q000696 cols=2
T A weighted alias splits invocations between two versions, here 90% to the stable version and 10% to the new one.
N c app "Callers" 0 0.5
N al lambda "Alias (weighted)" 1 0.5
N v1 lambda "Stable version" 2 0
N v2 lambda "New version" 2 1
E c>al
E al>v1 "90%"
E al>v2 "10%"

@Q000707 cols=3
T API Gateway serves the custom domain with an ACM certificate, and a Route 53 alias record points api.example.com at it.
N u users "Clients" 0 1
N r53 route53 "Route 53 alias\napi.example.com" 1 1
N api apigw "API Gateway\ncustom domain" 2 1
N acm acm "ACM certificate" 2 2 ! the certificate comes from ACM
E u>r53
E r53>api
E acm>api -- x

@Q002098 cols=2
T An HTTP API JWT authorizer validates the Cognito user pool token and rejects requests without one, with no custom code.
N spa app "Single-page app" 0 0
N up cognito "User pool (issuer)" 0 1
N api apigw "HTTP API\n(JWT authorizer)" 1 0
N be lambda "Backend" 2 0 ! the backend is typically Lambda
E spa>up "sign in"
E spa>api "Bearer token"
E up>api -- x "issuer / JWKS"
E api>be "valid only"

@Q002101 cols=2
T The backend issues a short-lived presigned URL, and the browser uploads the file directly to the private bucket without passing bytes through the API.
N br client "Browser" 0 0
N api apigw "API + Lambda" 0 1 ! the backend is an API Gateway and Lambda app
N s3 s3 "Private bucket" 1 0
E br>api <> "1. ask for URL\n2. presigned URL"
E br>s3 "3. PUT file"
