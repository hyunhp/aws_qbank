@Q000865 cols=2
T Feature Store keeps engineered features once: the offline store feeds training for many models and the online store serves the same values at low latency.
N fe glue "Feature pipelines" 0 0.5 ! features are often engineered with Glue or processing jobs
N fs sagemaker "Feature Store" 1 0.5
N off s3 "Offline store\n(training)" 2 0 ! the offline store lives in S3
N on sagemaker "Online store\n(inference)" 2 1
E fe>fs
E fs>off
E fs>on

@Q000956 cols=3
T A multi-model endpoint hosts hundreds of same-framework models behind one endpoint, loading each from S3 on demand.
N c app "Customers' calls" 0 1
N mme sagemaker "Multi-model\nendpoint" 1 1
N m1 s3 "model-a.tar.gz" 2 0 ! model artifacts are stored in S3
N m2 s3 "model-b.tar.gz" 2 1 ! model artifacts are stored in S3
N m3 s3 "... hundreds" 2 2 ! model artifacts are stored in S3
E c>mme "TargetModel"
E mme>m1 -- "load on demand"
E mme>m2 --
E mme>m3 --

@Q000962 cols=2
T The few-MB model runs inside a Lambda function behind API Gateway, so the backend pays only per request.
N be app "Mobile backend" 0 0
N api apigw "API Gateway" 0 1
N lam lambda "Lambda with\nscikit-learn model" 1 1
E be>api "few calls/min"
E api>lam

@Q000969 cols=2
T The endpoint runs in the bank's VPC with security groups limiting what containers reach, and callers invoke it through an interface endpoint.
G vpc vpc "Bank VPC" 0-1 0-1
N app app "Applications" 0 0
N ep endpoint "sagemaker-runtime\nendpoint" 1 0
N smep sagemaker "Endpoint containers\n(security groups)" 1 1
N ok s3 "Approved resources" 0 1 ! approved resources such as S3
E app>ep
E ep>smep
E smep>ok "allowed only"

@Q000973 cols=2
T API Gateway passes each request to Lambda, which calls InvokeEndpoint on the SageMaker runtime and returns the prediction.
N u users "Web users" 0 0
N api apigw "API Gateway" 0 1
N lam lambda "Lambda" 1 1
N sm sagemaker "Real-time endpoint" 1 0
E u>api
E api>lam
E lam>sm "InvokeEndpoint"

@Q000979 cols=2
T An EventBridge rule on object-created events under the prefix starts the SageMaker pipeline automatically.
N s3 s3 "New labeled data\n(S3 prefix)" 0 0
N eb eventbridge "EventBridge rule" 0 1
N pl sagemaker "SageMaker Pipeline" 1 1
E s3>eb "object created"
E eb>pl "start execution"

@Q000983 cols=2
T A weekly EventBridge schedule starts routine retraining, and a second rule on the Model Monitor drift alarm starts it out of cycle.
N sch eventbridge "Weekly schedule" 0 0
N mm sagemaker "Model Monitor" 0 1
N al cloudwatch "Drift alarm" 1 1
N rule eventbridge "Rule on alarm" 2 1
N pl sagemaker "Retraining\npipeline" 2 0
E sch>pl "weekly"
E mm>al
E al>rule
E rule>pl "on drift"

@Q000991 cols=2
T Data capture feeds Model Monitor, where Clarify bias drift compares production bias metrics against the baseline and alerts on drift.
N ep sagemaker "Credit endpoint\n(data capture)" 0 0
N s3 s3 "Captured data" 0 1 ! captured data is stored in S3
N cl sagemaker "Clarify bias drift\n(Model Monitor)" 1 1
N al cloudwatch "Alert" 1 0
E ep>s3
E cl>s3 -- x
E cl>al "threshold crossed"

@Q001016 cols=2
T Studio runs in VPC-only mode, and PrivateLink interface endpoints carry every call to the SageMaker APIs without touching the internet.
G vpc vpc "VPC (Studio VPC-only)" 0-1 0-1
N st sagemaker "Studio notebook" 0 0
N ep endpoint "Interface endpoints\n(api, runtime)" 1 0
N sm sagemaker "Training +\ninference APIs" 1 1
E st>ep
E ep>sm "PrivateLink"

@Q001019 cols=2
T CodeBuild and CodePipeline use service roles with temporary credentials and assume narrowly scoped roles in the dev and prod accounts.
G tool account "Tooling account" 0 0
N cp codepipeline "CodePipeline /\nCodeBuild roles" 0 0
G dev account "Dev account" 1 0
N rd iam "Scoped deploy role" 1 0
G prd account "Prod account" 1 1
N rp iam "Scoped deploy role" 1 1
E cp>rd "AssumeRole"
E cp>rp "AssumeRole"

@Q001020 cols=3
T Images come from a private, scanned ECR repository, and a verification stage blocks unapproved images before SageMaker deployment.
N cb codebuild "Build" 0 0 ! the pipeline builds images with CodeBuild
N ecr ecr "Private ECR\n(scan on push)" 0 1
N vf codebuild "Verify stage" 0 2 ! verification runs as a pipeline build step
N sm sagemaker "SageMaker endpoint" 1 2
E cb>ecr "push"
E ecr>vf "scan results"
E vf>sm "approved only"

@Q002144 cols=2
T Asynchronous inference takes the large image via S3, processes it for up to 20 minutes, notifies through SNS, and scales to zero when idle.
N c app "Client" 0 0
N in s3 "Input (800 MB)\nin S3" 0 1
N ai sagemaker "Async endpoint\n(scales to zero)" 1 1
N out s3 "Output in S3" 2 1
N sns sns "SNS notice" 2 0
E c>in "upload"
E in>ai "queued request"
E ai>out
E ai>sns
E sns>c -- via=2,-0.3;0,-0.3 "done"

@Q002146 cols=2
T Deployment guardrails shift traffic to the new model in canary or linear steps and roll back automatically if an alarm fires.
N ep sagemaker "Endpoint update\n(guardrails)" 0 0.5
N old sagemaker "Current fleet" 1 0
N new sagemaker "New model fleet" 1 1
N cw cloudwatch "Latency / error\nalarms" 2 1
E ep>old hv "shrinking"
E ep>new hv "growing"
E cw>new -- x "rollback trigger"

@Q002150 cols=2
T Network isolation blocks all container network calls, while SageMaker itself reads the training data privately through the S3 gateway endpoint.
G vpc vpc "VPC" 0-1 0-1
N tj sagemaker "Training job\n(network isolation)" 0 0
N net internet "Internet" 0 1
N gep endpoint "S3 gateway endpoint" 1 0
N s3 s3 "Training data" 1 1
E tj>net -- "blocked"
E tj>gep "data via SageMaker"
E gep>s3
